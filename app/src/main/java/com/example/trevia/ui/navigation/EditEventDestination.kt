package com.example.trevia.ui.navigation

object EditEventDestination
{
    const val ROUTE = "event_detail"
    const val EVENT_ID_ARG = "eventId"

    const val eventWithArgs = "$ROUTE/{$EVENT_ID_ARG}"
}